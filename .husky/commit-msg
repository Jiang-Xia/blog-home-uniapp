#!/usr/bin/env sh
# 同 pre-commit：避免 Windows GUI 下 npx 报 Could not determine Node.js install directory
cd "$(dirname "$0")/.." || exit 1
export PATH="$PWD/node_modules/.bin:$PATH"
commitlint --edit "$1"
